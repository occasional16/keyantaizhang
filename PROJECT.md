# 科研台账 (keyantaizhang) Project Context

> 本文件由项目自行维护。公共模板同步不会覆盖它。

## 产品边界

- **项目简介**：科研台账 (keyantaizhang) 是一套专为高校与科研院所 PI（课题组长/导师）、学术秘书、科研财务助理及研究生打造的**课题组学术成果智能整理与多维统计工作台 (Research Group Scholarly Output & Ledger Hub)**。官方标语为：*“课题组学术家底，一账摸清、一键直出。”*
- **核心用户与使用场景**：
  - 高校/院所课题组负责人 (PI)、学术科研秘书、实验室行政助理、硕博研究生；
  - 核心场景：年终科研成果统计与绩效考核、学科评估成果摸底、职称申报与评审材料准备、科研项目中期与结题成果汇总、课题组历史学术成果数字化归档。
- **核心价值**：
  - 彻底解决多源文献数据库（WOS、EI、知网、Scopus）导出格式异构、跨库重复收录、作者姓名缩写与别名多样、正式出版年份跨期、非本组成员干扰等痛点；
  - 提供**一键自动化**处理流程：全格式拼音消歧认领、DOI + 纯净题目双重跨库去重、SCI+EI 复合收录智能标记、双工作表交付大表直出（入库成果 + 未认领排除明细）、100% 真实逐行动态统计；
  - 100% 纯本地离线运行，保障课题组未公开学术成果与师生档案的隐私与数据安全。
- **非目标**：
  - 不做替代高校官方科研管理系统 (SRMS) 或财务 ERP 的机构级系统；
  - 不做公有云 SaaS 平台，不强制要求连接外网或上传数据到第三方服务器；
  - 不做泛化的日常文献阅读、批注或社交社区工具；
  - 不做依赖复杂环境（如 Node.js / Python / 外部数据库）的重型部署方案，保持极简开箱即用。

---

## 平台与兼容范围

- **主开发与验收平台**：Windows 10 / Windows 11 (64-bit)。
- **运行环境与宿主应用**：
  - Microsoft Excel 2016 / 2019 / 2021 / Microsoft 365 (32-bit / 64-bit 桌面版)；
  - 必须在 Excel 中启用宏（VBA），并允许访问 VBA 工程对象模型（用于源码热更新）；
  - 严禁任何绝对路径依赖，全流程基于 `ThisWorkbook.Path` 动态自适应相对路径。
- **编码与文件规范**：
  - VBA 源码模块 (`vba_modules/*.bas`) 使用 **GBK / CP936** 编码，确保在 Windows 简体中文版 Excel VBE 中不出现乱码；
  - 文档与配置文件 (`*.md`, `*.json`) 使用 **UTF-8 (无 BOM)** 编码。
- **明确不支持或尚未承诺的范围**：
  - 明确不支持 macOS 版 Excel（由于缺少 Windows 专有 COM/VBE 对象模型支持）；
  - 明确不支持移动端 Excel (iOS / Android) 及网页版 Office 365；
  - 明确不支持 WPS Office 在未安装完整 VBA 支持库环境下的运行。

---

## 架构与技术约束

- **架构设计（双核解耦与分层引擎）**：
  - **热更底座 (`vba_modules/Mod_Sync.bas`)**：专职负责秒级无损热重载所有 VBA 模块并重置控制台面板，实现“代码与数据分离”；
  - **总控引擎 (`vba_modules/Mod0_ControlPanel.bas`)**：负责交互面板渲染、状态看板扫描、业务全流程调度与交付报告输出；
  - **拼音引擎 (`vba_modules/Mod1_TeacherPinyin.bas`)**：读取 `config/teachers_profile.xlsx`，构建全格式拼音、缩写变体与检索特征库；
  - **清洗去重引擎 (`vba_modules/Mod2_CleanRawData.bas`)**：实现 WOS/EI/CNKI 多源抽取、正式出版年份过滤、消歧认领、DOI + 归一化题目双重去重、复合收录标记及双工作表直出。
- **关键数据约束**：
  - **双工作表交付标准 (`papers_final_merged.xlsx`)**：
    - `Sheet 1` (`【课题组入库成果】`)：标准 8 大核心字段（序号、论文题目、期刊名称、卷、期、作者、收录类型、影响因子）；
    - `Sheet 2` (`【未认领排除成果】`)：8 大核心字段（含原库原始作者全文及影响因子），供查漏补缺溯源核对；
  - **期刊影响因子智能匹配**：基于 `config/journal_if.xlsx` 的 `Journal Impact Factor` 列进行期刊名称归一化匹配，自动填充影响因子；
  - **100% 真实动态统计**：控制台看板数据全部基于真实逐行扫描计算，严禁任何历史比例推算与数量硬编码；
  - **正式出版年份基准**：严格以 `PY` / `Publication year` / `Year-年` 为过滤标准，防止在线预发表跨年干扰。
- **数据源只读保护**：
  - `raw_data/` 下所有原始导出数据（WOS.txt, EI.xlsx, CNKI.xls 等）均视为只读源，处理过程中绝不篡改。

---

## 验证与测试方法

### 1. 自动化工程规范验证
```powershell
# 运行项目模板治理漂移与清单检查
& "D:\AI\agent-skills\skills\bootstrap-code-project\scripts\manage_project_template.ps1" -Action Check -TargetPath "."
```

### 2. 交互工作台冒烟测试与业务验证
1. **环境准备与热更验证**：
   - 打开 `console_dashboard.xlsm`；
   - 点击顶部 **`[ 重置与同步代码 ]`**，观察提示框确认模块导入无误，面板状态刷新正常。
2. **端到端流程验证**：
   - 确认 `raw_data/` 中存在标准测试数据（WOS.txt、EI.xlsx、CNKI.xls），`config/teachers_profile.xlsx` 中存在成员档案；
   - 在控制面板设定测试年份区间（如 `2026-01-01` ~ `2026-12-31`）；
   - 点击右上角 **`[ >>> 一键自动化执行全流程 <<< ]`**；
   - 验证控制台状态更新为“处理完成”，且各阶段数据源统计正常显示。
3. **交付产物与一致性核验**：
   - 打开生成的 `papers_final_merged.xlsx`；
   - 检查 `Sheet 1`（入库成果）是否仅包含课题组成员姓名，格式符合 7 列规范；
   - 检查 `Sheet 2`（排除成果）是否完整记录未认领条目与原始作者；
   - 核对控制台看板显示的入库篇数、排除篇数、各类收录分布与大表记录数绝对 100% 一致。

---

## 发布信息

- **产品与包标识**：`keyantaizhang` (科研台账)
- **产物形态**：离线绿色运行包（包含 `console_dashboard.xlsm`, `vba_modules/`, `config/teachers_profile.xlsx`, `raw_data/`, `docs/`, `README.md`, `CHANGELOG.md`）
- **分发渠道**：GitHub Releases 绿色压缩包、课题组内部分发
- **发布门禁**：遵循 `docs/release.md` 规范，必须先在干净环境中完成全流程冒烟测试并校验 SHA-256。
