# AGENTS.md

## 项目依据

- 开始任务前阅读根目录 `PROJECT.md`，以其中的产品边界、兼容范围、技术约束和验收命令为项目专属依据。
- `PROJECT.md` 由当前项目维护；公共模板同步不得覆盖它。
- 以当前需求和经核验的最佳设计为最高依据；当前代码和历史实现只作为需要核查的证据，不因其已经存在就默认正确或必须延续。

## 开发原则

- 开发者的初步需求可能不完整或不符合最佳实践；主动指出问题并完善方案。只有会实质改变产品行为、范围或关键取舍时才暂停确认，不机械照做，也不把纯实现细节转交开发者决策。
- 选择完整满足当前需求的最简单持久方案，避免计划日后替换的权宜实现以及推测性的抽象和配置。
- 优先采用小步、端到端的方式交付；如果增量修改会保留错误结构、累积兼容层或增加长期复杂性，应重构或整体重写。
- 明确分离职责；只有在确实降低耦合或复杂性时才增加模块和间接层。
- 优先复用平台、框架和现有依赖的稳定能力；新增依赖前先核验必要性。
- 兼容性不是默认约束。只保护 `PROJECT.md`、当前任务或开发者明确指定的外部契约和不可恢复资产；直接移除过时的内部实现，不添加无明确需求的兼容层、回退逻辑或并行路径。
- 设计获批不等于实现获批；未经授权的方案只记录和讨论，不自动转为代码变更。

## 工作规范

- 仅修改当前任务所需内容，并保留开发者已有的无关改动。
- 创建、更新或执行 `docs/dev/` 中的工作文档前，先阅读 `docs/dev/README.md`。
- 大范围重构或整体重写前，明确当前需求、需要保护的契约与资产以及验收标准。
- 新增、升级或删除依赖前，先核验现有能力；显著影响架构、运行时、许可证或任务范围时必须获得明确授权。
- 执行破坏性操作、安装全局软件、提交、推送、创建 Release 或发布产物前，必须获得明确授权。
- 代码变更优先使用仓库已经提供的检查、测试和构建命令；仅修改文档时，检查内容、链接和差异。
- 完成任务时说明修改内容、验证结果和遗留风险。
